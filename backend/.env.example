# Backend environment example

# Comma-separated allowed origins for CORS/Socket.IO
CLIENT_URLS=http://localhost:5173

# Port to run the FastAPI/Socket.IO server
PORT=3000

# Optional custom path for SQLite database
# DB_PATH=./polls.db
